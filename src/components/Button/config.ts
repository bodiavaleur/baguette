import {theme} from '~config/theme';

export const titleColors = {
  default: theme.colors.white,
  text: theme.colors.text,
  primary: theme.colors.primary,
  danger: theme.colors.dangerRed,
  warning: theme.colors.warning,
};
